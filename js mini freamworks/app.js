let options = {
    strings: ['Matematika, Iqtisod, manaviyatshunoslik, vatan tuyg`usi', 'Milliy Universitet,  '],
    typeSpeed: 60,
    startDelay: 500,
    backSpeed: 40,
    backDelay: 700,
    loop: true,
    loopCount: Infinity,
};

let typed = new Typed('.element', options);